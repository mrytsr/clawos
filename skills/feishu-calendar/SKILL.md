# feishu-calendar

封装飞书日历功能，默认使用用户195 (唐君行)。

## 功能

- 添加日历事件 (add)
- 查询日历事件 (list)
- 删除日历事件 (delete)

## 配置

默认用户ID: `195` (唐君行)

## 使用方法

### 添加日历

```
feishu_calendar add --title "会议" --date 2026-02-15 --time 14:30 --duration 60 --desc "讨论项目"
```

### 参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--title` | 事件标题 | 必填 |
| `--date` | 日期 (YYYY-MM-DD) | 明天 |
| `--time` | 时间 (HH:MM) | 10:00 |
| `--duration` | 持续时间(分钟) | 30 |
| `--desc` | 事件描述 | 空 |
| `--uid` | 用户ID | 195 |

### 示例

```bash
# 明天10点的30分钟提醒
feishu_calendar add --title "喝水提醒"

# 指定时间和日期
feishu_calendar add --title "团队会议" --date 2026-02-15 --time 14:30

# 自定义时长和描述
feishu_calendar add --title "面试" --date 2026-02-16 --time 10:00 --duration 60 --desc "候选人: 张三"
```

## 返回格式

成功返回 JSON:

```json
{
  "success": true,
  "event_id": "7ea8c143-7276-4a6f-96d5-a2d12758d8b5",
  "summary": "📅 OpenClaw日历测试",
  "start_time": "2026-02-09T10:00:00",
  "url": "https://applink.feishu.cn/..."
}
```

## 错误处理

失败时返回:

```json
{
  "success": false,
  "error": "用户未绑定飞书或token已过期"
}
```

## 技术说明

- 依赖: `/home/tjx/xiandan/lib/fs_calendar.py`
- 依赖: `/home/tjx/xiandan/lib/fs_access_token.py`
- Token管理: 自动从数据库读取并刷新
